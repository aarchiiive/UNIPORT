import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  // text
  text: {
    fontSize: 60,
    marginBottom: 4,
    backgroundColor: 'transparent'
  },
  gradientText: {
    borderRadius: 30,
  },
});