
export const toHome = ({ navigation }: { navigation: any; }) => {
  navigation.navigate('Home');
};

export const toStart = ({ navigation }: { navigation: any; }) => {
  navigation.navigate('Start');
};

export const toLogin = ({ navigation }: { navigation: any; }) => {
  navigation.navigate('Login');
};

export const toPolicyAgreement = ({ navigation }: { navigation: any; }) => {
  navigation.navigate('PolicyAgreement');
};

export const toSignUp = ({ navigation }: { navigation: any; }) => {
  navigation.navigate('SignUp');
};

export const toMain = ({ navigation }: { navigation: any; }) => {
  navigation.navigate('Main');
};

export const toNotifications = ({ navigation }: { navigation: any; }) => {
  navigation.navigate('Notifications');
};

export const toProfile = ({ navigation }: { navigation: any; }) => {
  navigation.navigate('Profile');
};